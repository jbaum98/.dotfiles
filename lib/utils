add_path() {
    export PATH="$1:$PATH"
}

add_existing_path() {
    if [ -d $1 ] ; then
        add_path $1
    fi
}

add_existing_paths() {
    lines=($@)
    for line in $lines; do 
        add_existing_path $line
    done
}

exists() {
    hash $1 2>/dev/null
}

# vim:filetype=sh
