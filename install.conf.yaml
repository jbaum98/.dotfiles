- shell:
    - [git stash, Stashing local modifications]
    - [git pull, Updating dotfiles]
    - [git submodule update --init --recursive, Updating submodules]
    - [git stash pop, Restoring local modifications]
    - mkdir -p $HOME/.config

- clean: ['~', '~/.config']

- link:
    ~/.bashrc: bashrc
    ~/.bash_profile: bash_profile
    ~/.profile: profile
    ~/.zprofile: zprofile
    ~/.emacs.d: emacs.d/
    ~/.exrc: exrc
    ~/.gitconfig: gitconfig
    ~/.githelpers: githelpers
    ~/.spacemacs: spacemacs
    ~/.tmux.conf: tmux.conf
    ~/.vim: vim/
    ~/.config/nvim: vim/
    ~/.vimrc: vim/init.vim
    ~/.zshrc: zshrc
